"""Multi-train planners used as proposal agents.

`blackbox/` is AI4REALNET/flatland-blackbox's solver code, vendored; `replan.py` is
the glue that runs it on a live episode. Plan: docs/plans/proposal-agents-roadmap.md.
"""
