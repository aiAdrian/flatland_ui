-r requirements.txt
pytest>=8.3.0
pytest-asyncio>=0.24.0
# starlette's TestClient needs an HTTP client: >=1.3 prefers httpx2 and
# deprecates httpx, older versions only know httpx. Both are declared so the API
# tests collect on either. Without one of them, 11 test files fail at import
# with a misleading "requires the httpx2 package" error.
httpx>=0.27.2
httpx2>=2.10.0
ruff>=0.7.0
